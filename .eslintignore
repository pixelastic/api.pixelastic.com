# Hidden files (.prettierrc.js, .huskyrc.js, etc) are ignored by default by ESLint
# The following line will unignore them
!.*

# node_modules in subfolders are not ignored by default
node_modules/

# Testing the functions on CircleCI creates compiled version of the functions in
# this folder
.netlify/

